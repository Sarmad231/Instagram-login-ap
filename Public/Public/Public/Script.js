const loginForm=document.getElementById("login-form"),signupForm=document.getElementById("signup-form"),showSignup=document.getElementById("show-signup"),switchText=document.getElementById("switch-text"),messageDiv=document.getElementById("message");function toggleForms(){"none"===loginForm.style.display?(loginForm.style.display="block",signupForm.style.display="none",switchText.innerHTML=`Don't have an account? <a id="show-signup">Sign up</a>`,document.getElementById("show-signup").addEventListener("click",toggleForms)):(loginForm.style.display="none",signupForm.style.display="block",switchText.innerHTML=`Have an account? <a id="show-login">Log in</a>`,document.getElementById("show-login").addEventListener("click",toggleForms))}showSignup.addEventListener("click",toggleForms);const params=new URLSearchParams(window.location.search);params.has("message")&&(messageDiv.textContent=params.get("message"),messageDiv.className="true"===params.get("success")?"success":"error");
